# 事件抽取改造说明

## 1. 背景与目标

当前事件抽取模块主要围绕通用五维事件要素展开：

- `subject`：行动主体
- `action`：行为或动作
- `technology`：涉及技术
- `scene`：应用场景
- `time`：时间信息

这套结构适合完成“从文本到事件”的基础结构化任务，也能支撑后续候选对象成形、弱信号评分和报告生成。但如果目标进一步升级为“基于弱信号的技术预见”，仅依赖通用五元组会存在信息粒度不足、证据可追溯性不足、早期变化特征表达不足等问题。

本次改造目标是将事件抽取从“通用事件抽取”升级为“弱信号感知型事件抽取”，让事件层直接承载技术预见所需的关键线索，同时保持对历史 `events.json/csv` 的兼容。

## 2. 为什么需要改造

### 2.1 弱信号不是普通技术事实

弱信号通常不是已经成熟、广泛传播的技术结论，而是早期、模糊、小众、跨界、低频但可能具有方向性意义的变化迹象。通用五元组只能表达“谁做了什么技术”，很难表达：

- 技术对象是否具体
- 机制变化在哪里
- 能力边界是否发生变化
- 是否处于早期探索阶段
- 是否出现跨领域迁移
- 是否有小众主体或低关注来源支撑
- 证据是否足够可追溯

因此，弱信号识别不能只在后处理阶段补规则，事件抽取阶段也应主动捕获这些预见线索。

### 2.2 当前后处理压力过大

现有流程中，`candidate_units`、`observation_scopes`、弱信号提示字段和事件质量字段大多在抽取后由规则或后续模块推断。这样做的优点是稳定、可控，但也会带来两个问题：

- 对复杂论文、专利、研报文本，规则可能难以准确还原真实技术机制。
- 当原始五元组过粗时，后续候选成形、主题细化和反向验证会被早期信息损失限制。

将机制、对象、任务、数据模态、证据片段等信息前置到抽取阶段，可以降低后续模块的猜测成本。

### 2.3 技术预见需要可解释证据链

技术预见不是只输出一个候选名称，还需要说明“为什么这个方向值得跟踪”。如果事件层缺少证据片段、置信度和弱信号原因，后续报告只能从结构化字段反推理由，容易出现解释泛化或证据不够贴合的问题。

因此，事件抽取应输出可追溯证据和可解释判断，为质量评分、候选聚合、时间验证和报告生成提供更稳的证据底座。

## 3. 改造原则

1. **兼容优先**：保留现有五个核心字段，历史数据仍可读取和继续分析。
2. **渐进增强**：新增字段先作为增强信息进入流程，不立即替代现有规则链路。
3. **证据优先**：每条事件必须尽量保留原文证据片段，便于人工复核和报告引用。
4. **多事件支持**：允许一篇文本产生多条事件，避免复杂文本被压缩成单个粗粒度事件。
5. **版本可追踪**：新增事件 schema 版本字段，便于历史实验对照。
6. **可评估**：每项改造都要有可观察指标，而不是只凭主观感觉判断效果。

## 4. 目标事件 Schema

建议保留现有字段，并新增一组弱信号预见字段。

### 4.1 兼容字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 来源文档或事件关联 ID |
| `subject` | string | 行动主体 |
| `action` | string | 行为动作 |
| `technology` | list[string] | 涉及技术 |
| `scene` | string | 应用场景 |
| `time` | string | 文本中提及的时间 |
| `date` | string | 来源日期 |
| `source_type` | string | 来源类型，如 paper、patent、news、report |
| `title` | string | 来源标题 |

### 4.2 新增预见字段

| 字段 | 类型 | 说明 | 为什么需要 |
| --- | --- | --- | --- |
| `event_schema_version` | string | 事件结构版本，如 `weak_signal_event_v2` | 支持历史实验和版本对照 |
| `event_id` | string | 单条事件唯一 ID | 支持一文多事件 |
| `event_type` | string | research、patent_application、prototype、deployment、investment、policy、market_signal 等 | 区分技术事实和扩散迹象 |
| `technical_object` | string | 具体技术对象或对象化表达 | 降低候选名称过泛的问题 |
| `mechanism` | string | 核心技术机制 | 支撑候选成形和技术链映射 |
| `task` | string | 解决的任务或能力方向 | 识别能力边界变化 |
| `data_modality` | list[string] | 数据模态，如 video、sensor、trajectory、multimodal | 捕捉跨模态与新数据基础 |
| `method` | list[string] | 方法特征，如 causal、retrieval-based、reinforcement | 捕捉机制差异 |
| `capability_change` | string | 相比已有技术的能力变化 | 服务技术预见中的“变化”判断 |
| `problem_solved` | string | 解决的问题或约束 | 避免只抽技术名，忽略需求牵引 |
| `maturity_stage` | string | idea、lab、prototype、pilot、early_adoption、scaled | 判断信号所处阶段 |
| `novelty_signal` | string | 新颖性线索 | 识别非主流或新组合 |
| `adoption_signal` | string | 扩散或采用迹象 | 识别从研究到产业的转折 |
| `cross_domain_signal` | string | 跨领域迁移线索 | 捕捉弱信号常见的跨界组合 |
| `weak_signal_reason` | string | 为什么这条事件像弱信号 | 提升可解释性 |
| `uncertainty` | string | 不确定性或证据限制 | 防止过度判断 |
| `evidence_span` | string | 原文证据片段 | 支撑追溯和人工复核 |
| `confidence` | float | 抽取置信度，0 到 1 | 支持质量评分和过滤 |

### 4.3 候选成形辅助字段

这些字段可由 LLM 抽取，也可由现有规则补全，建议先作为可选字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `mechanism_core_tokens` | list[string] | 机制核心词 |
| `task_constraint_tokens` | list[string] | 任务约束词 |
| `object_modifier_tokens` | list[string] | 技术对象修饰词 |
| `data_modifier_tokens` | list[string] | 数据模态修饰词 |
| `method_modifier_tokens` | list[string] | 方法修饰词 |
| `candidate_units` | list[object] | 候选成形单元，可兼容现有流程 |

## 5. 目标输出结构

建议将抽取结果从“每篇文本一个事件对象”升级为“每篇文本一个事件数组”。

```json
{
  "doc_id": "paper_001",
  "events": [
    {
      "event_schema_version": "weak_signal_event_v2",
      "event_id": "paper_001_evt_001",
      "subject": "某研究团队",
      "action": "提出",
      "technology": ["world model", "robot planning"],
      "scene": "机器人长期任务规划",
      "time": "2025",
      "event_type": "research",
      "technical_object": "用于机器人规划的世界模型",
      "mechanism": "基于长期记忆的交互式环境建模",
      "task": "长期几何一致性规划",
      "data_modality": ["video", "trajectory"],
      "method": ["memory-augmented", "distillation"],
      "capability_change": "提升长时序交互环境中的一致性",
      "problem_solved": "缓解实时世界模型中的记忆衰减和误差漂移",
      "maturity_stage": "prototype",
      "novelty_signal": "将长期上下文记忆用于实时交互世界建模",
      "adoption_signal": "",
      "cross_domain_signal": "视频生成方法迁移到机器人环境建模",
      "weak_signal_reason": "低频机制组合，具备跨模态和跨场景迁移潜力",
      "uncertainty": "尚缺少产业部署证据",
      "evidence_span": "原文中描述模型通过动态重建历史帧维持长期几何一致性。",
      "confidence": 0.82
    }
  ]
}
```

落地到当前 DataFrame 时，可以继续展开为一行一个事件，保持 `events.json` 为 records 数组。

## 6. 实施方案

### 阶段一：新增兼容型 Schema

改造点：

- 在事件抽取模块中新增 `WEAK_SIGNAL_EVENT_SCHEMA_VERSION` 常量。
- 扩展事件字段列表，保留旧字段并加入新增字段。
- 增加事件字段规范化函数，对缺失字段填充空值或默认值。
- 保证历史五字段事件仍能进入后续流程。

为什么这样做：

- 先建立稳定的数据契约，避免后续模块各自临时判断字段。
- 兼容历史数据，方便复现实验和 A/B 对照。

建议验收：

- 旧版 `events.json` 能被读取。
- 新版抽取结果包含 `event_schema_version`。
- 缺失新增字段不会导致 Pipeline 报错。

### 阶段二：改造 LLM 抽取 Prompt

改造点：

- 将抽取提示从五元组扩展为弱信号感知 schema。
- 要求模型返回 JSON 数组，允许一篇文本输出 0 到 N 条事件。
- 明确要求 `evidence_span` 必须来自原文，不允许编造。
- 要求模型区分“明确证据”和“推断判断”。

为什么这样做：

- 一篇论文或专利经常包含多个机制、对象或应用场景，单事件会压缩信息。
- 证据片段可以显著提升事件质量评分和报告可信度。
- 把弱信号原因前置，可以和现有规则判断形成互补。

建议验收：

- 批量抽取结果数量不再强制等于输入文本数量，而是按事件展开。
- 每条事件都有 `event_id`、`id` 和 `evidence_span`。
- 对无有效技术事件的文本允许返回空数组。

### 阶段三：增强本地规则回退

改造点：

- 更新模拟抽取函数，使其也能输出新版字段。
- 使用现有技术词典补充 `mechanism`、`technical_object`、`data_modality`、`method`。
- 本地回退不追求完全准确，但要保证字段完整和流程可跑。

为什么这样做：

- API 不可用或实验环境不稳定时，流程仍可运行。
- 字段形态一致，减少后续模块分支判断。

建议验收：

- 无 API 配置时仍能生成新版事件表。
- 本地回退事件不会破坏候选成形、事件质量评分和报告生成。

### 阶段四：候选成形接入新增字段

改造点：

- 候选成形优先读取 `technical_object`、`mechanism`、`task`、`data_modality`、`method`。
- 如果新增字段为空，继续走现有 `candidate_units` 和规则抽取逻辑。
- 将 LLM 抽取的候选辅助字段与规则生成结果合并，并记录来源。

为什么这样做：

- 可以逐步利用新版抽取能力，而不是一次性替换现有稳定规则。
- 对复杂文本，LLM 给出的机制和对象可减少规则误判。
- 对简单文本，现有规则仍能提供稳定兜底。

建议验收：

- 新版事件能生成更具体的候选对象。
- 旧版事件仍能通过规则链路生成候选。
- 候选结果中能区分 `source_extraction_mode` 或类似字段。

### 阶段五：事件质量评分升级

改造点：

- 将 `evidence_span`、`confidence`、`uncertainty` 纳入事件质量评分。
- 增加“预见相关性”评分维度，关注新颖性、跨界性、早期扩散迹象。
- 低质量事件不删除，只降低权重或标记风险。

为什么这样做：

- 弱信号具有不确定性，直接删除低置信事件可能损失早期信号。
- 质量评分应服务排序和人工复核，而不是简单过滤。

建议验收：

- `event_quality.json/csv` 增加或体现预见相关指标。
- 报告证据优先引用高质量、可追溯事件。

### 阶段六：历史数据迁移与实验对照

改造点：

- 增加历史事件补全脚本或函数，将旧五字段事件补齐为新版 schema。
- 对补全字段标记来源，例如 `schema_migration_mode = "legacy_backfill"`。
- 同一批历史数据运行旧抽取结果、新补全结果、新抽取结果三组实验。

为什么这样做：

- 不能把“字段补全带来的变化”和“抽取模型能力提升”混在一起。
- 历史数据是验证弱信号预见效能的重要基线。

建议验收：

- 历史事件文件可一键补齐 schema。
- 实验结果能区分旧版、迁移版和新版。

## 7. 实验设计

建议至少做三组对照：

| 实验组 | 数据来源 | 目的 |
| --- | --- | --- |
| A 组 | 历史五字段事件 | 作为旧基线 |
| B 组 | 历史事件补齐新版 schema | 验证兼容补全效果 |
| C 组 | 新版弱信号感知抽取 | 验证新抽取模块效果 |

建议观察指标：

- 有效事件数
- 一文多事件比例
- 事件平均质量分
- 有证据片段事件占比
- 候选对象具体性
- 候选去重后保留率
- 弱信号候选数量
- 高质量证据占比
- 报告中可追溯证据覆盖率
- 人工复核通过率

## 8. 风险与控制

| 风险 | 表现 | 控制方式 |
| --- | --- | --- |
| LLM 过度推断 | 输出原文没有的弱信号原因 | 强制 `evidence_span` 来自原文，并区分事实与推断 |
| 字段变多导致噪声增加 | 后续候选变散 | 新字段先作为辅助，不直接替代原规则 |
| 历史数据不兼容 | 旧 events 无法跑新流程 | 保留旧字段，新增迁移补齐逻辑 |
| 多事件导致数量膨胀 | 候选聚合压力变大 | 设置每文最大事件数和置信度阈值 |
| 弱信号被质量评分压制 | 早期信号证据少，分数偏低 | 质量评分用于排序和风险提示，不直接删除 |

## 9. 建议开发任务拆分

1. 新增事件 schema 常量和字段规范化函数。
2. 扩展事件缓存字段列表，保证新增字段能保存到 `events.json`。
3. 改造单条和批量 LLM 抽取 Prompt，支持 JSON 数组和一文多事件。
4. 改造 API 抽取解析逻辑，展开多事件并生成稳定 `event_id`。
5. 更新本地模拟抽取，使其输出新版字段。
6. 更新候选成形逻辑，优先读取新版机制、对象、任务、数据模态字段。
7. 更新事件质量评分，纳入证据片段、置信度和预见相关性。
8. 增加历史事件 schema 补齐工具。
9. 增加 A/B 实验脚本或说明，输出核心对照指标。
10. 更新 README 或版本说明，标注事件 schema 变化。

## 10. 推荐落地顺序

优先级建议如下：

1. **先做兼容 schema 和字段保存**：这是后续所有工作的基础。
2. **再做 Prompt 和解析改造**：让新版抽取真正产生增量信息。
3. **随后接入候选成形**：验证新增字段是否提升候选质量。
4. **最后做质量评分和实验对照**：用指标判断是否值得进一步深化。

这样做可以降低一次性大改的风险，也方便随时回退到现有稳定流程。

## 11. 最小可行版本

如果希望先快速验证效果，可以只实现最小版本：

- 保留旧五字段。
- 新增 `event_schema_version`、`event_id`、`technical_object`、`mechanism`、`capability_change`、`weak_signal_reason`、`evidence_span`、`confidence`。
- 支持一篇文本输出多条事件。
- 后续流程暂时不强依赖新增字段，只保存并展示。

最小版本完成后，即可用历史数据和新抽取结果做第一轮对照，判断新版事件抽取是否真的提升弱信号识别和技术预见效果。
